# Server-only environment (never VITE_-prefixed — those are client-visible).
# Copy this file to .env and fill it in; deploy platforms provide the real
# values as environment variables.

# Signs the session cookie (32+ random characters). Generate one:
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
SESSION_SECRET=
