# packageExtensions works around @kobalte/solidbase importing "resolve"
# without declaring it, which breaks pnpm installs. Remove once fixed upstream.
packageExtensions:
  "@kobalte/solidbase":
    dependencies:
      resolve: "^1.22.8"
allowBuilds:
  esbuild: true
